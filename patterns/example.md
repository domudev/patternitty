---
name: example
type: feedback
state: noticed
occurrences: 1
applies_to:
  tool: "*"
  glob: "**/*"
  project: "*"
target: null
---

This is a reference copy showing the schema in use — real patterns live in
`${PATTERNITTY_HOME:-~/.patternitty}/patterns/`, not in this repo. Delete this
file's counterpart there once you have real distilled patterns.

**Why:** new patternitty installs otherwise have no worked example of the
frontmatter shape.
**How to apply:** n/a — this is documentation, not a rule to compile.
