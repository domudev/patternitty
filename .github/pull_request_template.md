## What & why

<!-- one or two lines: what this changes and the reason -->

## Checklist

- [ ] `for t in tests/test_*.py; do uv run "$t"; done` passes locally
- [ ] Bumped `version` in `.claude-plugin/plugin.json` if this is a release-worthy change
- [ ] Docs updated (README / SKILL / _SCHEMA) if behavior changed
